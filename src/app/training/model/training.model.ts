export interface Training {
  id: number;
  date: Date;
  name: string;
  additionalInfo: string;
}

export interface TrainingRow {
  id: number;
  date: Date;
  name: string;
  additionalInfo: string;
}
